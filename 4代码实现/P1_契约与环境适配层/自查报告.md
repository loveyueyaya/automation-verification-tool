# P1 自查报告（契约 + 环境适配层）

- 日期：2026-09-16 ｜ 阶段：P1 ｜ 架构依据：架构设计 v3 + 用户评分意见
- 产物：contracts/（9 文件）+ env_adapter/（7 文件）+ tests/（3 文件）+ MIGRATION.md
- 源码权威位置：ui-toolbox 技能根（本目录为归档副本）

## 1. 测试结果：27/27 通过

```
Ran 27 tests in 0.001s  OK
test_select_flow 13 例：STRONG≥2→HUMAN / 其他failure→EXCEPTION /
                       cache+exact_repeat→CACHE / diff None|significant→LLM / 其余→NORMAL
test_stable_id   8 例：三层生成 / none空段 / 定位L1>L2>L3 / 缓存L3>L2>L1 /
                       碰撞消歧 / 逐层回退 / 非法格式
test_state_hash  6 例：ocr不参与 / 文本变化→hash变 / 2%网格容差 /
                       target_hint圈定 / 全角→半角规范化
```

## 2. env_adapter 独立运行冒烟（真实本机）

```
probe_env: framework=None, integrity=medium, input=handle_post, capture=mss, 30ms
route_reason: 焦点不可靠（远程软件接管）        ← 检测到 wujie/GameViewer 自动路由
cache: put→命中 / TTL 过期→None ✓
3 个契约脚本 import 正常（locate/timeline/uitool）
CONTRACT_VERSION = 2026.09.16-P1 ✓
```

## 3. 用户 4 点澄清落地核对

| 澄清点 | 落地 | 验证 |
|---|---|---|
| 元素 id 用途区分 | stable_id.resolve_for_action（L1>L2>L3）/ resolve_for_cache（L3>L2>L1）；空段 none | 单测通过 |
| state_hash target_hint | select_key_elements 圈定规则 + 2% 网格 + 规范化 | 单测通过 |
| 跨进程 trace 偏移 | MIGRATION 第 3 节：5 处 uitool._sub() 锚点 + 报告说明行 | 锚点已列（P2 实施） |
| select_flow 5 流 | contracts/flow.py + VerificationResult.retries + light_check/exact_repeat | 13 例通过 |

## 4. P1 验收对照

- [x] 至少 3 脚本实际用枚举（locate 3 处 / uitool 14 处 / timeline 接收方）
- [x] stable_id 三层/用途/消歧/回退
- [x] state_hash 参与不参与/容差/变化
- [x] select_flow 5 流全分支
- [x] 字符串字面量违规规则（MIGRATION 硬规则）
- [x] 跨进程 trace 锚点 5 处
- [x] env_adapter 独立运行

## 5. 遗留（诚实声明）

- 性能预算表仍全"待校准"（首轮 ≥50 帧实测前不作门禁）
- 跨进程 trace 传播为 P2 实施（锚点已列）
- 剩余 9 脚本（shot/ocr/sendinput/cache/perfmon/diagnose/testbar/ocr_bench/wgc_probe/env）P2 契约化
- 测试控制台 exe 内嵌逻辑 = 已知拷贝违规，P2 薄壳化
