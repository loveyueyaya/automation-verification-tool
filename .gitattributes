# 行尾（CRLF/LF）转换策略
#
# 背景：2026-09-19 推送时出现
#   warning: in the working copy of '09-feedback/timeline.jsonl', LF will be replaced by CRLF the next time Git touches it
# 本项目的 .jsonl / log.txt 是"逐行追加的事件流与决策日志"，属证据类文件，
# 不希望 git 在 checkout 时改写行尾字节（会让审计比对出现无意义差异）。
# 故对本类文件关闭文本转换（-text 表示按二进制处理、不做行尾归一化）。

*.jsonl -text
log.txt -text

# 本文件自身也不做行尾转换（避免自己触发同类告警）
.gitattributes -text

# 其余文本文件保持仓库默认行为（不在此处强制 eol，避免一次性大范围改写）。
#
# 另：本仓库已在仓库级关闭 autocrlf（见 git config core.autocrlf=false，
# 系统级 gitconfig 里是 true），双重保证证据类文件不被改写字节。
