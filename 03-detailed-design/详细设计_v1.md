# 详细设计 v1（P2 分层落地方案）

> 日期：2026-09-17 ｜ 版本：v1
> 依据：架构设计 v3.1 + P1 落地实际 + 现有目录结构
> 状态：待评审（尚未动 04-implementation）

---

## 一、7 层目录结构定义（目标形态）

```
04-implementation\
└── P2-layers\                         ← P2 起按层拆目录（P1 目录保留不动，迁入源标记）
    ├── orchestrator\                  ← 层1 编排
    │   ├── task_queue.py              # 任务队列
    │   ├── window_lock.py             # 窗口互斥锁
    │   ├── human_intervene.py         # 人工介入
    │   └── flow.py                    # select_flow（自 contracts/flow.py 迁入）
    ├── env_adapter\                   ← 层2 环境适配（✅ P1 已落地，仅补充）
    │   ├── env_probe.py / env_cache.py / input_router.py / capture_router.py
    │   └── focus_manager.py / fallback_policy.py
    ├── hardware+perception\           ← 层3 感知
    │   ├── capture.py                 # 截图引擎（自 scripts/shot.py 迁入）
    │   ├── input.py                   # 输入注入（自 scripts/sendinput.py 迁入）
    │   ├── ocr_engine.py              # PaddleOCR 单例（自 scripts/ocr.py 迁入）
    │   ├── window.py / uia.py         # 窗口枚举 / 控件树
    │   ├── element_detect.py          # 元素检测（自 env.pick_target 迁入）
    │   ├── snapshot.py                # 状态快照（state_hash 输入）
    │   ├── dpi.py                     # DPI 转换
    │   └── resource_monitor.py        # 资源占用监控
    ├── state_cache+planner\           ← 层4 决策与缓存
    │   ├── state_store.py             # state_hash 存储（自 contracts/state_hash.py 迁入）
    │   ├── rule_cache.py / element_cache.py / decision_cache.py
    │   ├── prompt_templates.py
    │   ├── flow_planner.py / exception_handler.py
    │   └── llm_client.py              # 双后端抽象（v1 云端豆包 / v2 ollama）
    ├── locator+executor\              ← 层5 执行
    │   ├── resolver.py                # 元素 id → 坐标（自 contracts/stable_id.py + scripts/locate.py 迁入）
    │   ├── handle_match.py / uia_match.py / coord_match.py
    │   ├── action_runner.py           # 动作执行（自 scripts/sendinput.py 迁入）
    │   ├── retry_policy.py            # 幂等重试
    │   ├── humanize.py                # 人类化延迟
    │   └── process.py                 # 进程/端口（自 env.process_count / port_listen 迁入）
    ├── verifier\                      ← 层6 校验
    │   ├── state_diff.py / ocr_check.py / screenshot_diff.py / business_assert.py
    ├── observability\                 ← 层7 观测
    │   ├── logger.py                  # 毫秒级日志（自 scripts/timeline.py 迁入）
    │   ├── metrics.py / report.py / replay.py
    ├── contracts\                     ← 横切①（✅ P1 已落地，不动）
    ├── config\                        ← 横切②（P2 新建）
    │   └── budget.yaml / fallback.yaml / paths.yaml
    └── regression\                    ← 横切③（P3 新建，smoke 门禁）
```

**迁移原则**：P1 的 `scripts\` 与 `env_adapter\` **保留不动**；P2 分层 = 复制/移动 + 改 import，行为不变（MIGRATION.md 逐项记录）。

---

## 二、每层首个实现模块清单

| 层 | 首个模块 | 来源 | 完成标志 |
|---|---|---|---|
| 层1 编排 | `orchestrator/flow.py` | contracts/flow.py（select_flow 纯函数） | 单测通过（5 流全分支） |
| 层2 环境适配 | ✅ 已落地 | — | 无需首模块 |
| 层3 感知 | `element_detect.py` | env.pick_target + noise | 窗口选择命中率 ≥99% |
| 层4 决策 | `decision_cache.py` | cache.py + state_hash | 缓存命中 → 动作出流 |
| 层5 执行 | `action_runner.py` | sendinput.py 路由 | 三类注入路由正确 |
| 层6 校验 | `state_diff.py` | validators.py | diff 计算单测 |
| 层7 观测 | `logger.py` | timeline.py | 毫秒时间戳 + trace_id 链 |
| 横切 config | `budget.yaml` | v3 第六节预算表 | 加载即校验 schema |

---

## 三、config 横切包 yaml 结构示例

### budget.yaml（性能预算表，P1 后待实测校准）

```yaml
# 性能预算表：任何值在完成 ≥50 帧实测回填前不得作为门禁阈值
budget:
  env_probe_first:    { p95_ms: 50,   measured_ms: null, calibrated: false }
  env_probe_cached:   { p95_ms: 0,    measured_ms: null, calibrated: false }
  perception:         { p95_ms: 200,  measured_ms: null, calibrated: false }
  state_query:        { p95_ms: 10,   measured_ms: null, calibrated: false }
  llm_decision:       { p95_ms: 2000, measured_ms: null, calibrated: false }  # 云端 v1；本地 ollama v2 = 500
  locate:             { p95_ms: 5,    measured_ms: null, calibrated: false }
  execute:            { p95_ms: 100,  measured_ms: null, calibrated: false }  # 人类化延迟不计入
  verify:             { p95_ms: 150,  measured_ms: null, calibrated: false }
  full_chain:         { p95_ms: 500,  measured_ms: null, calibrated: false }
on_over_budget: warn_and_record      # 或 escalate_exception（按层配置）
```

### fallback.yaml（降级阈值，声明式）

```yaml
fallback:
  ocr_gpu_fail:        { action: switch_cpu, note: "GPU 不可用降级 CPU，限区域，慢 5-10 倍" }
  uia_b_not_ready:     { action: suggest_force_renderer, note: "env_adapter 探测→建议；orchestrator 执行" }
  cdp_unreachable:     { action: route_sendinput_shell_ui, note: "页面 CDP 失败；外壳 UI 走 SendInput/句柄直投" }
  llm_backend_none:    { action: rule_only, note: "backend=none 纯规则决策（缓存+模板），不瘫痪" }
  retry_policy:        { max_retries: 2, backoff_ms: [200, 800] }
```

### paths.yaml（路径配置，默认 F 盘）

```yaml
paths:
  workspace:    "F:/自动化验证工具"
  models:       "F:/models"                # PaddleOCR 等（默认 F 盘；C 盘只放程序本体）
  caches:       "F:/自动化验证工具/.cache"
  trace:        "F:/自动化验证工具/04-implementation/P1-contracts-env-adapter/timeline"
  program:      "C:/Program Files/..."     # 程序本体（C 盘）
```

---

## 四、跨进程 trace 协议（v3 第四节落地版）

```
父进程 uitool.py
  ├─ spawn 前 记录 t_parent0
  ├─ 子进程 ocr.py --image x.png --trace-id <tid>        (或 UI_TRACE_ID=<tid>)
  ├─ 子进程入口 记录 t_child0；offset = t_parent0 - t_child0
  ├─ 子进程 span.ts = time.time() + offset（归一父时钟）
  └─ 全部写入同一 session 的 timeline.jsonl（不新建 session 目录）
     首行声明 {pid, parent_trace_id, clock_offset}

字段契约：
  record(ts, ts_source, span_id, parent_span, trace_id, src, conf, chain, action, result)

报告展示：offset 含子进程启动延迟 N ms → 展示行注明"子进程启动延迟 N ms 已计入偏移"
断链检测：孤儿 span（无父）→ 标黄 + "跨进程断链"告警
```

**落地锚点（P2）**：`uitool._sub()` 全部子进程调用加 `--trace-id`（约 6 处）；`timeline.record` 增加 `parent_span` 字段。

---

## 五、幂等性分级表

| 等级 | 定义 | 动作示例 | 重试策略 |
|---|---|---|---|
| L0 | 天然幂等 | 查询、读状态、截图 | 可无限重试（同输入同输出） |
| L1 | 幂等（有副作用但可重复） | 设置开关、粘贴文本 | retries ≤ 2，成功后校验 |
| L2 | 条件幂等（依赖前置状态） | 点击按钮（前置=按钮存在） | retries ≤ 2 + 每步 light_check |
| L3 | 非幂等（不可安全重放） | 提交表单、转账类 | **禁止自动重试** → EXCEPTION / HUMAN 流 |

规则：
- L0/L1 失败 → NORMAL 流重试（backoff 200/800ms）
- L2 失败 → 重试前先 light_check（目标元素指纹 + 窗口 rect），失效则走 EXCEPTION
- L3 失败 → 直接 HUMAN 流（人工介入），retries 字段置位记录
