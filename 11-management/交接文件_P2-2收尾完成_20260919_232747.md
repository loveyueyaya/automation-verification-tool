# 交接文档：P2-2 收尾完成

> 生成时间：2026-09-19 23:27:47 +0800（系统命令 date 取得）
> 交接范围：2026-09-19 晚间会话（18:19 ~ 23:2x）全部工作
> 权威源：`F:\自动化验证工具\`（git main，本地 = 远端 = `a580e75`）

---

## 1. 当前状态

| 项 | 状态 |
|---|---|
| 测试 | **56/56 全绿**（`Ran 56 tests in 0.368s / OK`，退出码 0，2026-09-19 23:27 实测） |
| 最新 commit | `76cab87`（本地 = 远端 main；工作区干净；文档回写三笔 d04fdc2 / f23a4d1 / 76cab87 已补） |
| 最近 5 条 commit | 见文末 |
| P2-2 | **彻底完成**（骨架迁移 + 收尾清单 5 项全部闭环） |
| 下一阶段 | P2-3（未开始，待用户指令） |

## 2. P2-2 收尾 5 项分别做了什么

| # | 事项 | 改动文件 | 验证 |
|---|---|---|---|
| 1 | 调用方 import 指向 P2-layers | `P1-toolbox-legacy\scripts\locate.py` / `timeline.py` / `uitool.py`、`tests\` 4 文件（引导代码重写，相对路径定位，改名免疫；埋入 flag `UITOOL_CONTRACTS_SOURCE=p1\|p2`） | 56 全绿 0.449s |
| 2 | 删 contracts shim | 删除 `contracts\__init__.py`（**用 python os.remove，不用 git rm**——git rm 曾触发整目录消失事件，见第 3 节） | 56 全绿 0.372s；`import contracts` 实测解析自 P2-layers |
| 3 | P1 目录改名 | `git mv P1-contracts-env-adapter → P1-toolbox-legacy`（原名删 shim 后名不副实） | 56 全绿 0.351s |
| 4 | feature flag | 新增 `P1-toolbox-legacy\restore_p1_shim.py`：p1 模式前一键恢复 shim（git 历史提取优先 `1ae52c5`，内置全文兜底）；闭环实测 restore → p1 模式 56 全绿 → `--remove` → p2 模式 56 全绿 | 双向各 56 全绿 |
| 5 | BUG-2 / BUG-4 | **BUG-2 接线**：`uitool.py` see() 调 env_adapter.env_probe，探测结果（security/framework/focus_reliable/input_method/capture_method/dpi/probe_ms）写入审计时间线；**BUG-4 修复**：`env_probe.py` 改声明式 `FRAMEWORK_RULES`（framework 只由目标窗口自身进程判定，security 只影响 focus_reliable） | 56 全绿 0.353s + 实跑（timeline step=env_profile 含完整 JSON；framework 不再恒为 wujie） |

## 3. 已知问题、风险点、未完成事项

### 已知问题（未定位根因）
- **目录消失事件**（19:50）：`git rm` 删 shim 后 `04-implementation` 整目录从磁盘消失（硬删、回收站 0 条、当时审计为盲区）。`git checkout HEAD` 零丢失恢复后改用 python os.remove 顺利完成。**删除者未定位**。
- **Temp 目录脚本偶发消失**：C:\Users\...\Temp 下新写入的 .py 有一次立即消失（规避：临时脚本放 `.workbuddy\tmp\`）。与目录消失可能同源。

### 防护（已就位）
- `F:\自动化验证工具` 全树已设 **Delete 审计 SACL**（Everyone，DELETE+删除子树，成功+失败，目录+文件继承）——**今后任何删除都会在安全日志留 4663 事件（含进程名）**，复发即可归因。证据：`10-env-baseline\_evidence\09b_project_sacl_20260919.txt`。

### 风险点
- auditpol File System 审核子状态未能在远程会话确认（`auditpol /get` 报 0x57）——P2-3 开工前建议控制台复验。
- classic PAT 权限偏大（含 delete_repo）——建议收敛为仓库级 fine-grained 最小权限。
- `security=wujie`（系统有远程控制软件）仍会使 focus_reliable=False → 走句柄直投+MSS 降级；**真实键鼠验证前先关远程软件**（unverified-paths：SendInput/dxcam 主路径从未真机验证）。

### 未完成事项
- ~~P2-2 的 tag（`ui-toolbox-P2-2-*`）未打~~ → ✅ **已打**（2026-09-20 00:17）：`ui-toolbox-P2-2-20260919` → `028ffc2`，已推送远端（用户裁定打点与推送）
- 09-18 的日报（`daily\feedback_20260918.md`）缺失待补。

## 4. P2-3 要做什么，第一步从哪开始

**P2-3：testbar 薄壳改造 + 单源复用 + 跨进程 trace**（验收：代码去重率 ≥60%、trace 全链路贯通）
1. 抽共享 testbar（测试控制台与 uitool 的公共逻辑薄壳化）
2. 公共工具函数归 core/utils
3. 全链路 trace_id/span_id（`TraceSpan` 需增加 `span_id`/`parent_span` 默认字段，向后兼容——contracts/models.py）
4. 核心接口契约化

**第一步**：把 `测试控制台`（pyc 备份中有 testbar.cpython-313.pyc，源码位置见 `10-env-baseline\_evidence` 与 P1-pyc-cache-backup）与 `P1-toolbox-legacy\scripts\` 的重复逻辑做差异盘点（grep 对照），列出"单源化清单"后再动代码。

## 5. 接手人第一步应该做什么（具体到命令）

```bash
# 1) （2026-09-20 复验：coreutils 已修复，此行可省；保留写法无害）
export PATH="/c/Program Files/Git/usr/bin:$PATH"

# 2) 确认基线（必须 56/56 OK）
cd "F:/自动化验证工具/04-implementation/P1-toolbox-legacy"
"C:/Program Files/Python313/python.exe" -m unittest discover -s tests

# 3) 确认 git 状态（本地=远端=a580e75 或更新，工作区干净）
cd /f/自动化验证工具 && git status --porcelain && git log --oneline -5 && git ls-remote origin main

# 4) 读状态与规则
#    11-management\状态面板.md → 11-management\参考资料\management-index.md
#    → 11-management\参考资料\本地离线自动化测试工具_开发前盘点_v2.md（§六 硬规则必读）
#    → 11-management\交接记录\（本文档）
```

**注意**：一切判断基于本机实跑；任何删除/覆盖/移动前先建 cache-manager 快照（`make_cache.py --task ... --paths ...`）；**不用 git rm**。（2026-09-20 复验：Temp 脚本消失问题已消失，临时脚本可正常使用；若复发仍建议放 `.workbuddy\tmp\`。）

## 6. 本次对话使用的云端大模型型号 / 上下文占用

| 项 | 值 |
|---|---|
| 云端大模型型号 | **无法从本机实测获取**（运行环境不暴露模型标识；按规则 1 不猜测，以平台界面显示为准） |
| 上下文占用总量 / 占用率 | **无法实测**（本环境无 token 计数接口）。可数事实：本会话约 50+ 轮消息/工具调用往返，含三次大批量文件读取与多次长输出；占用率以平台 UI 显示为准，**标注：不清晰** |

## 附：最近 5 条 commit

```
76cab87 P2-2 收尾文档回写补齐：验收报告 §5 收尾补记 + 日报 §6 晚间会话 + 各 index 状态同步
f23a4d1 11-management 目录重组 + 状态面板 + P2-2 收尾交接文档
d04fdc2 P2-2 收尾文档回写：11-management 全量更新 + 各子目录 index 同步
a580e75 取证归档：目录消失事件审计查询输出 + 项目树 Delete 审计 SACL 配置证据（盲区补齐）
028ffc2 P2-2 收尾：调用方 import 指向 P2-layers + 删 contracts shim + P1 目录改名 P1-toolbox-legacy + feature flag + BUG-2/BUG-4 修复
```

---

## 7. 11-management 目录命名规则与结构调整（用户 2026-09-19 23:5x 裁定 + 手动调整）

**命名规则**：
1. **子目录只能使用英文名**（如 `Reference materials`）；
2. 子目录下的 `.md` 文件可按功能用**中文或英文**命名（如 `本地离线自动化测试工具_开发前盘点_最新版.md`）。

**目录结构调整（用户手动）**：交接文档移至根目录（`交接文件_*.md`）；原 `参考资料/` 更名 `Reference materials/`；原 `历史归档/` 取消（盘点 v1 移入 Reference materials）。两个盘点已合并为 `本地离线自动化测试工具_开发前盘点_最新版.md`（v1/v2 原文已并入后移除，git 历史可查）。
- xlsx 处置补充：project-phase-progress.xlsx 由用户手动移出并裁定'没有多大作用'，恢复副本已于 2026-09-20 00:0x 删除（git 历史可查）。进度速览职责由本交接文档与状态面板承担。
### 1.1 新手四坑速查（2026-09-20 00:06 复验，三项已由用户在其他会话修复）

| 坑 | 复验结果 |
|---|---|
| Git Bash 缺 coreutils（ls/date 失效） | ✅ **已修复**：裸 `ls`/`date` 直接可用 |
| 裸 `python` 指向 3.13.12 | ✅ **已修复**：裸 `python --version` = **3.13.14** |
| Temp 下新脚本偶发消失 | ✅ **已消失**：写入→跨进程复查→清理全程正常 |
| PowerShell 工具 stdout 不回传 | ⬜ 未验证（已无关紧要：Bash 路线全通） |

> 结论：新会话可正常使用 `ls`/`date`/`python`；PATH 补丁写法保留为无害保险。

### 1.2 未决事项清单（截至 2026-09-20 00:06）

> 📌 **基线口径勘误（2026-09-20 00:48 加注，原文 56 单测描述不改）**：本文档与 §1 表中"56 单测"为 **P2-2 时点的历史基线**。
> P2-3 起单测基线升级为 **70**（新增 `tests/test_trace_span.py` 3 例 + `tests/test_core_utils.py` 11 例），
> 实测 `Ran 70 tests in 0.462s / OK`；**本次改动的 commit：`63f9378`**（P2-3 Step1~Step3，已推送远端 main）。
> 当前口径以 `11-management/状态面板.md` 为准；**历史正文保留 56 不改**（项目规则：历史记录保持原样，变更以注记说明）。

| # | 事项 | 状态 |
|---|---|---|
| 1 | P2-2 的 tag（`ui-toolbox-P2-2-*`）未打 | ✅ **已完结**（2026-09-20 00:17）：用户裁定打点 `028ffc2`，annotated tag `ui-toolbox-P2-2-20260919` 本地+远端均已就位（用户裁定：推送远端） |
| 2 | 目录消失事件（09-19 19:50）根因未定位 | SACL 已就位，复发即归因 |
| 3 | classic PAT 权限收敛（现含 delete_repo） | 建议换仓库级 fine-grained 最小权限 |
| 4 | 09-18 日报（daily\feedback_20260918.md）缺失 | 待补 |
| 5 | SendInput / dxcam 主路径从未真机验证 | P3 smoke 必须含"关远程软件验证主路径"用例 |
