接下来的规划总览（10轮）
【本文件更新：2026-09-19 19:5x（第四次更新：P2 暂停解除，**P2-2 收尾清单 5 项全部完成**，详见第二节末尾"收尾完成记录"）；历史：18:2x 第三次（状态复验）/ 17:2x 第二次（用户裁定）】

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
〇、用户裁定记录（2026-09-19 17:2x）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. **contracts shim 去重 + P1 目录改名 → 并入 P2-2 收尾**（不在暂停期单独动）
   - 涉事：`04-implementation/P1-contracts-env-adapter/contracts/`（仅 1 个 shim）与 `P2-layers/contracts/`（9 文件）重复；
     以及 `P1-contracts-env-adapter/` 目录名已名不副实（内含 scripts/tests/evidence）
   - 落地时机：P2-2 第 5 步（改调用方 import）+ 第 7 步（删 shim）同批执行，避免同一批文件改两遍
2. **关闭行尾转换** —— 已执行并复验：
   - 新增 `.gitattributes`：`*.jsonl -text`、`log.txt -text`、`.gitattributes -text`
   - 仓库级 `git config core.autocrlf false`（系统级 gitconfig 为 true，故按仓库覆盖）
   - 复验：`git check-attr text` 三项目标文件均为 `unset`；`git add -A` 零 CRLF 告警；工作区无 churn
3. **P2 系列任务保持暂停**，本文件只更新状态与决定，不启动任何开发动作

附：新增技能 `github-remote`（用户在 00 号文件夹创建）—— 已同步到 AppData 加载目录，凭证存本地忽略目录
   （`.credentials/` 已被根 `.gitignore` 忽略，`git check-ignore` 复验通过；AppData 侧同步时已排除该目录）。
   凭证状态更新：第一枚 fine-grained PAT（`github_pat_…`）实测 **401 Bad credentials**（格式合法但被拒，疑似已撤销）；
   用户随后提供 **classic PAT（`ghp_…`，40 位）** → 实测 **200 OK**，login=loveyueyaya（id 37976486），
   scopes 含 `repo / workflow / admin:repo_hook / delete_repo` 等 → **当前可用**。
   ⚠ 该 token 权限偏大（含 delete_repo），建议后续按需收敛为最小集（仓库级 fine-grained PAT：Contents+PR+Actions 读写）。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
〇之二、当前进度快照（实测，非规划）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
| 轮次 | 阶段 | 状态 | 实测依据 |
| --- | --- | --- | --- |
| 第1轮 | P2-1 | ✅ 已完成（1 项与规划有出入，见下） | 56 单测全绿；OpenCV 收敛为 2 包；pynvml 替换完成（21ms vs nvidia-smi 147ms）；BUG-1/BUG-3 已修并实跑验证 |
| 第2轮 | P2-2 | ✅ **已完成（含收尾清单 5 项，2026-09-19 21:4x 闭环）** | `P2-layers/`（contracts 9 + env_adapter 7）+ `P1-toolbox-legacy/`（原 P1 目录已改名，shim 已删、调用方 import 已指 P2-layers、flag restore_p1_shim.py、BUG-2 接线/BUG-4 修复）；56 单测 5 次全绿；三笔提交 f2bf819/028ffc2/a580e75 已推送 |
| 第3轮 | P2-3 | 🟡 **进行中**（Step1/1b/2/3 已完成，Step4「testbar 改引用」留下一轮） | 单测基线 **56 → 70**（`Ran 70 tests in 0.462s / OK`）；trace 贯通实跑通过；详见 §三·补 P2-3 进展记录 |
| 第4~10轮 | P3-x / P4-x | ⬜ 未启动（暂停中） | — |

环境与工程侧（规划外，09-19 已完成）：文件丢失事故取证与根因定位、覆盖法修复、损失评估（结论不恢复）、
API 逐一验证（46 项 0 失败）、环境全面检测、目录树整理归类、C1/C2 元数据修复、**整理结果逐项验证 123 项全过**、
**已提交并推送 GitHub（远端 main = `f5c180c`）**。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
〇之二·补、新会话状态复验（2026-09-19 18:19~18:2x，只读核验，未启动任何开发动作）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
全部结论来自本机实跑（命令 + 原始输出 + 退出码）：

| 项 | 实测结果 |
| --- | --- |
| git | 工作区干净（`git status --porcelain` 零输出）；HEAD = 远端 main = `c66e5d9`（含 17:2x 凭证/行尾/日志四次提交）；tags 仍为 2 个（ui-toolbox-P1-20260916 / P1-patch1-20260917） |
| 56 单测 | `Ran 56 tests in 0.413s — OK`，退出码 0 |
| P2-layers | contracts 9 文件 + env_adapter 7 文件在位，与 09-19 迁移记录一致 |
| P1 shim | `P1-contracts-env-adapter/contracts/__init__.py` 在位（sys.modules 整体替换方案；文件头已注明第 7 步删除时机与回滚方式） |
| BUG-2（收尾 #5） | 复现确认：`env_adapter` 在 04-implementation 全树**零代码引用**（仅 P2-layers/contracts/enums.py 两处 docstring 提及，无 import） |
| BUG-4（收尾 #5） | 复现确认：`probe_env()` 实测 `security=wujie` → `focus_reliable=False` → 降级 `HANDLE_POST + MSS`（本轮无窗口调用 framework=None；带窗口时 framework='wujie' 误判待修） |
| 盘点 v2 存放位置 | 实测已在 `11-management\本地离线自动化测试工具_开发前盘点_v2.md`——第四节参考清单中"待用户确认存放位置"事实上已落地（本轮不改，请用户下次过目时顺手确认） |

结论：**P2-2 收尾清单 5 项的前置条件全部具备**（基线全绿、远端同步、shim/新包结构在位、两个 BUG 可复现）。
P2 仍处暂停；是否解除并按收尾清单开工，待用户裁定。本轮未改动任何代码，仅更新本文件状态。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
一、第1轮 P2-1（依赖清理 + 阻塞BUG修复）——已完成，含 1 项偏差
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
硬性规则（原样保留）：
1. 所有改动必须基于本机实跑验证，不允许凭空推测
2. 每改一个依赖/每修一个BUG，都要跑56个单测确认没回退
3. 本轮只做前置准备，不碰目录结构、不新增业务功能

任务与结论：
1. OpenCV依赖冗余清理 —— ⚠ 与规划不一致，按实测调整
   规划："统一保留 opencv-python，另外两个卸载"
   实测结论：**必须保留 opencv-contrib-python**
     · paddlex 3.7.2 的 ocr-core/base 等 extra 硬性要求 `opencv-contrib-python==4.10.0.84`
     · 该判断读的是 **dist 元数据**（is_extra_available），不是 import —— 删掉该 dist-info 会让 PaddleOCR 直接拒绝创建流水线（已实测复现）
   最终状态：opencv-python 4.10.0.84 + opencv-contrib-python 4.10.0.84（同版本，无冲突）；opencv-python-headless 5.0.0.93 已移除（旧包备份在 F:\wordbuddy\2026-09-18-21-23-01\_backup_opencv_stale\）
   遗留：僵尸 opencv_python-5.0.0.93.dist-info 已于 09-19 清理完毕
2. 显存监控方案替换 —— ✅ 完成
   ocr.py 已改 pynvml 原生 API（含 Windows nvml.dll 预加载兼容段）；实测读 12282 MiB，21 ms/次（nvidia-smi 147 ms/次）
   补记：nvidia-ml-py3 7.352.0 的 dist-info 曾在事故中丢失，09-19 已从回收站原样恢复
3. BUG-1（区域截图失效）—— ✅ 已修
   shot.py `_region_box`；实测 --region 200,200,320,200 → [320,200]（修复前恒为全屏 2560×1440）
4. BUG-3（OCR 每次 spawn 子进程）—— ✅ 已修
   get_ocr 单例 + ocr.py --serve 常驻；实测冷启动 8888 ms，推理 901 → 106 ms（裁剪 320×200 / 22 条）
5. BUG-2（env_adapter 零引用）、BUG-4（framework 误判）—— ⏸ 按规划本轮不修（BUG-4 实测仍为 framework='wujie'）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
二、第2轮 P2-2（七层目录骨架 + 迁移映射表）——基本完成，做法修订
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
实际做法（与规划差异）：
· 规划目录名 `P2-seven-layer-architecture/`（contracts/core/cli/config/tests/utils）→ 实际建的是 `P2-layers/`
· 原因：本轮只具备"契约层 + 环境适配层"的迁移条件；七层其余层尚无代码可放，先建空壳会误导
· 迁移方式：方案 B（P1 侧 `contracts/__init__.py` 改 shim，整体替换 sys.modules），env_adapter 整目录 git mv
· 已验证：迁移后 56 单测全绿；P2-layers 内 16 个文件行数与迁移前逐一比对一致

未完成（**用户 2026-09-19 裁定：不再单独立项，全部并入 P2-2 收尾，随 P2-2 解除暂停时一次性做完**）：

**P2-2 收尾清单（暂停解除后按序执行，共 5 项）**
| # | 事项 | 内容 | 备注 |
| --- | --- | --- | --- |
| 1 | 第 5 步：改调用方 import | `scripts/locate.py`、`timeline.py`、`uitool.py` 与 tests 加 sys.path 引导，指向 `P2-layers/` | 与 #3 改名同批做，避免改两遍 |
| 2 | 第 7 步 / shim 去重 | 删 `P1-contracts-env-adapter/contracts/` 的 shim；契约层唯一实现落在 `P2-layers/contracts/`（9 文件） | 需先 #1 完成，且 56 单测全绿 |
| 3 | P1 目录改名 | `P1-contracts-env-adapter/` 名不副实（内含 scripts/tests/evidence），改为名副其实的目录名 | 与 #1 同批；改名用 `git mv` 保历史 |
| 4 | feature flag | 一键切回 P1 旧路径 —— 属验收标准缺口，必须补 | 与 #2 联动（切回时 shim 需可恢复） |
| 5 | BUG-2 / BUG-4 | env_adapter 零引用、framework 被 wujie 污染 | 未随迁移处理，留在收尾同批 |

**✅ 收尾完成记录（2026-09-19 19:4x~19:5x，暂停解除后一次执行）**

| # | 结果 | 验证 |
| --- | --- | --- |
| 1 | 调用方 import 指向 P2-layers：`scripts/locate.py`、`timeline.py`、`uitool.py` + `tests/` 4 文件（相对路径引导，改名免疫），并埋入 feature flag：环境变量 `UITOOL_CONTRACTS_SOURCE=p1\|p2`（默认 p2） | 56 单测全绿 0.449s |
| 2 | shim 已删除（改用 python os.remove，规避 `git rm` 触发点——见下"目录消失事件"）；契约层唯一实现 = `P2-layers/contracts/` | 56 全绿 0.372s；`import contracts` 实测解析自 P2-layers |
| 3 | `P1-contracts-env-adapter/` → **`P1-toolbox-legacy/`**（git mv 保历史；删 shim 后原名名不副实） | 56 全绿 0.351s |
| 4 | feature flag 闭环：`restore_p1_shim.py`（git 历史提取优先，内置全文兜底）→ p1 模式 56 全绿 → `--remove` → p2 模式 56 全绿 | restore 实测命中 `1ae52c5` |
| 5 | **BUG-2 接线**：`uitool.see()` 调 env_adapter.env_probe，探测结果（security/framework/focus_reliable/input_method/capture_method/dpi）写入审计时间线（实跑验证：timeline step=env_profile 含完整 JSON）；**BUG-4 修复**：env_probe 改声明式 `FRAMEWORK_RULES`（framework 只由目标窗口自身进程判定；security 只影响 focus_reliable），实跑验证 framework 不再恒为 wujie | 56 全绿 0.353s + 实跑 |

**⚠ 事件记录：目录消失与审计盲区（2026-09-19 19:50）**：首次执行 #2 用 `git rm` 删 shim 后，`04-implementation` 整目录从磁盘消失（硬删，回收站 0 条；当时 F 盘项目目录无删除审计，删除者无法定位）。已 `git checkout HEAD` 完整恢复（零丢失），改用 python os.remove 后 #2 顺利完成。**防护已补齐**：给 `F:\自动化验证工具` 全树设置可继承 Delete 审计 SACL（Everyone，成功+失败，含子目录文件）——今后任何删除都会在安全日志留 4663 事件（含进程名）。证据：`10-env-baseline\_evidence\09b_project_sacl_20260919.txt`；收尾前快照：`temp-cache\20260919_194705`。

执行约束（沿用环境硬规则）：每项完成后跑 56 单测；#1/#3 为 git 写操作，执行前建 cache-manager 快照并按串行策略执行。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
三、第3~10轮（原文保留，待启动）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
| 轮次 | 阶段 | 核心动作 | 类型 | 验收标准 |
| --- | --- | --- | --- | --- |
| 第3轮 | P2-3 | testbar薄壳改造 + 单源复用 + 跨进程trace | 重构 | 代码去重率≥60%、trace全链路贯通 |
| 第4轮 | P3-1 | UIA控件识别接入 + 库冲突分析 | 新功能 | UIA能拿到目标窗口控件树、与现有依赖无冲突 |
| 第5轮 | P3-2 | WGC高速截图接入（bettercam）+ 库冲突分析 | 新功能 | 截图速度实测≥60fps、高DPI无偏移 |
| 第6轮 | P3-3 | 四层识别裁决器 + 统一接口封装 | 架构整合 | HANDLE/UIA/CACHE/OCR四层回退链路全通 |
| 第7轮 | P3-4 | CDP浏览器通道接入 + 库冲突分析 | 新功能 | 能attach已有Chrome、能执行JS、能监听事件 |
| 第8轮 | P3-5 | YOLO接口预留 + 人类化操作 + 异常重试 | 新功能 | YOLO枚举+空接口就位、操作带随机延迟、失败自动重试 |
| 第9轮 | P4-1 | 全量smoke用例 + CI配置 | 工程化 | 3条smoke用例全过、CI流水线跑通 |
| 第10轮 | P4-2 | 基线冻结 + v1.0打tag + 文档收尾 | 交付 | 基线tag打好、README/CHANGELOG齐全 |

后续轮次的前置现状（实测）：
· P3-1：uiautomation 2.0.29 已装可用（深度3=11节点/18.4ms）；pywinauto 0.6.9 可作 fallback
· P3-2：现状 dxcam 后续抓帧 0.28 ms、mss 21.61 ms 可作对比基线；WgcCapture.exe 仍缺失
· P3-4：CDP 端口 9222/9223 实测不可达
· P4-2：建议在此轮用官方 wheel 做一次「版本对齐 + 覆盖法补齐」，把 09-19 事故的外围缺失一次性补平

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
三·补、P2-3 进展记录（2026-09-20 00:4x 实测，用户三条裁定后执行）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
**用户三条裁定（2026-09-20 00:3x）**
1. `core/utils` 落点 = **`P2-layers/core/`**（新建 core 包；不进 contracts、不建顶层 core）
2. 验收口径由「字面去重率 ≥60%」改为「**公共功能点中 ≥60% 落在 core/utils 且被单源引用**」（实测字面克隆为 0%，原口径无法达成）
3. **必须补单测**，允许破「56 单测」口径并同步文档

| 步骤 | 内容 | 结果 |
| --- | --- | --- |
| Step1 | `contracts/models.py` 给 `TraceSpan` 补 `span_id`/`parent_span`（默认空串、置末尾） | ✅ 56→56 仍全绿；旧 6 参构造与 `duration_ms` 实跑不变 |
| Step1b | 测试控制台源码入库 `04-implementation/testbar-console/`（6 文件，SHA256 与桌面原件一致，原件保留） | ✅ 已暂存 |
| Step2 | 新建 `P2-layers/core/`：`constants.py`（远控名单并集 11 项）+ `utils.py`（jdefault / keymap / proc / proc_stats / gpu / json 读写） | ✅ `gpu_total_mb=12282`、`remote_tools_running=['wujie']`、`F10→121` 实跑一致 |
| Step2 接线 | `locate.py`/`uitool.py` 删各自 `_jdefault` 改引用 `core.utils.jdefault`；`sendinput.py` 支持键名 | ✅ CLI 全通 |
| Step3 | `timeline.record` 增可选 span 三元组；`sendinput/ocr` 加 `--trace-id`；`uitool` 加 `set_trace/_tl/_sub` span 机制与 CLI `--trace-id` | ✅ 实跑：根 span `s0` → 子进程 span `s1`（parent=s0），业务事件结构不变 |
| Step3b | 补单测 `test_trace_span.py`(3) + `test_core_utils.py`(11) | ✅ **基线 56 → 70**，`Ran 70 tests in 0.462s / OK` |

**未完成**：Step4（testbar-console 改引用 env.py / env_probe / pynvml / core.utils）—— 用户裁定**留到下一轮单独做**（GUI 无单测覆盖，需真机 smoke）。
**验收口径达成度**：11 项公共功能点中已落 core/utils 且被引用 7 项（jdefault、keymap、proc、远控名单、proc_stats、gpu、json 读写）≈ 64%（≥60% ✅）；窗口信息/完整性判定属"改调 env.py、env_probe"，GDI 截图经评估**不并入**（与 mss 同路，属重复造轮子）。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
四、新增：环境硬规则（09-19 实测得出，所有轮次通用）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 禁止对大包执行 pip uninstall / pip install --force-reinstall
   实测：本环境删除经 genie-trash 逐个进回收站，47~126 ms/文件（cmd del 仅 0.74 ms/文件，慢 64~170 倍）
   → 数千文件的卸载必然超过命令超时被强杀 → 包被半删（回收站出现 ~umpy / ~addlepaddle-*.dist-info 等指纹）
   注：卸载 opencv-python 会连带删共享 cv2/；卸载 paddlepaddle(CPU) 会删共享 paddle/ 树打死 paddlepaddle-gpu
2. 修环境一律用「覆盖法」：pip download 取 wheel → 解压 → 只补缺失文件 → 覆盖写入（不删除）
3. 必须批量删除时：先把该条命令超时放大到 ≥1800 s，并先建 cache-manager 快照
4. 一切判断基于本机实跑：文件/包存在性用 os.path.exists / importlib.util.find_spec / 实跑验证，
   不得用 RECORD、README、索引描述替代实测（已出现"RECORD 说文件在、实际目录已空"的反例）
5. 用户提供的判断/状态同样要跑代码复验后再采信
6. 版本问题查官方手册与更新说明；不涉及本项目实际调用 API 则不做升级
7. 系统审计（File System 审核 + Python313 的 Delete SACL）长期保留，可归因删除进程

参考（均在本机）：
· 根因取证 10-env-baseline\env-incident_20260919_root-cause.md
· 损失评估 10-env-baseline\env-damage-assessment_20260919.md
· API 与冲突检测 10-env-baseline\env-api-and-conflict-check_20260919.md
· 开发前盘点 v2 （待用户确认存放位置）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
五、原始 10 轮明细（保留存档，任务文本未改动）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【P2-1 原文】依赖清理 + 阻塞BUG修复：OpenCV三包收敛为一包；删 subprocess nvidia-smi 改 pynvml；修 BUG-1/BUG-3；BUG-2/BUG-4 本轮不修。
【P2-2 原文】七层目录骨架 + 迁移映射表：建 P2-seven-layer-architecture/（contracts/core/cli/config/tests/utils）；13 脚本迁移映射表；原样搬迁只改 import；feature flag 一键切换；同步单测；顺手处理 BUG-2/BUG-4。
【P2-3 原文】testbar薄壳改造 + 单源复用 + 跨进程trace：抽共享 testbar；公共工具函数归 core/utils；全链路 trace_id/span_id；核心接口契约化；跑全量单测确认等价。
【P3-1 原文】UIA控件识别接入：库冲突分析（uiautomation vs pywinauto）；选型（主力 uiautomation + pywinauto fallback）；core/adapters/uia_adapter.py；SourceEnum 新增 UIA=2；记事本/计算器实测。
【P3-2 原文】WGC高速截图接入：库冲突分析（bettercam vs dxcam）；选 core/adapters/wgc_adapter.py；三层降级 WGC→MSS→GDI；多分辨率/多DPI/多屏/HDR 实测。
【P3-3 原文】四层识别裁决器 + 统一接口：HANDLE→UIA→CACHE→OPENCV→OCR 优先级；core/orchestrator/recognition_router.py；对外只暴露 find_element/click_element/input_text；预留 YOLO=6；各层与回退实测。
【P3-4 原文】CDP浏览器通道接入：websockets / pycdp / playwright attach 三方案分析选型；core/adapters/cdp_adapter.py；attach 已有 Chrome、执行 JS、监听事件、自动重连；只 attach 不 launch。
【P3-5 原文】人类化操作 + 异常重试 + 审计日志增强：贝塞尔轨迹、随机微延迟、打字随机间隔；重试 3 次+指数退避；审计含截图/控件树快照 + trace_id 检索；配置中心整合。
【P4-1 原文】全量smoke用例 + CI配置：3 条 smoke（记事本全链路 / 连续截图100张 / 四层回退）；接入 testbar；CI 配置（依赖安装→静态检查→单测→smoke→报告）；质量门禁（覆盖率≥70%、flake8/pylint 零错误、mypy 零错误）。
【P4-2 原文】基线冻结 + v1.0打tag + 文档收尾：全量回归；打 v1.0.0；README/CHANGELOG/docs/requirements.txt；性能基线报告（截图/OCR/UIA/四层裁决/内存显存）；项目总结报告。
