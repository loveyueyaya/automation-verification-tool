# 审计日志（步骤 5–8：全函数覆盖 + 字符串搜索 + 依赖比对 + 证据归档）

审计时间：2026-09-16（续第二批）
git 状态：**未执行任何 git push / sync**（保持停止）

---

## 步骤 5：等宽测试扩展到全部 9 个差异函数

### 5.1 追加测试（Edit 到 tests/test_rebuild_equiv.py）

新增 6 个测试类、20 个用例（9→29）：
- `TestLocByTemplateEquiv`（3）：阈值命中/低于阈值/图读取失败
- `TestTimelineShotEquiv`（1）：截图落盘 + 相对路径
- `TestTimelineReportEquiv`（2）：分阶段统计精确值 + >2s 异常标注 + HTML + 空时间线
- `TestAutoClickEquiv`（3）：无 expect 成功/定位失败记录/expect 命中
- `TestMainEnvEquiv`（1）：env 分支 JSON 输出
- `TestPickTargetEquiv`（3）：无规则/规则命中/全噪声
- `TestProcessCountEquiv`（2）：大小写不敏感/异常兜底
- `TestPortListenEquiv`（3）：psutil 命中/socket 兜底/双失败
- `TestWaitWindowReadyEquiv`（2）：响应即 True/超时轮询

### 5.2 首跑（4 处失败，如实贴出）

```text
Ran 29 tests in 0.186s
FAILED (failures=2, errors=2)
EXITCODE=1
```

| 失败 | 根因 | 性质 |
|------|------|------|
| test_responding_true / test_timeout_false_after_polling | `env.wait_window_ready` 用 `ctypes.wintypes.LRESULT()`，**Python 3.13.14 实测 wintypes 无 LRESULT** → AttributeError | **真实实现缺陷**（原始 pyc 亦如此写法，从未运行过故未暴露） |
| test_report_md_stats_and_slow_flag | 断言 P95=285.0，实际实现 `v[min(int(len*0.95),len-1)]` → len=2 → v[1]=300.0 | 我的测试期望算错 |
| test_shot_saves_png_relative_path | 断言 `startswith("shots/")`，Windows `os.path.relpath` 返回 `shots\...` | 我的测试平台假设错 |

### 5.3 修复

1. `env.py` wait_window_ready：`ctypes.wintypes.LRESULT()` → `ctypes.c_long()`（LRESULT 等价 c_long，加注释"修复自审计"）
2. 测试断言 P95 修正为 300.0；相对路径断言改 `startswith("shots" + os.sep)`；两处 open 改 with（消除 ResourceWarning）

### 5.4 修复后完整输出（29 用例）

```text
test_click_ok_without_expect ... ok
test_expect_ok_returns_after_verify ... ok
test_locate_fail_returns_error_and_records ... ok
test_cache_expired_re_enumerates ... ok
test_cache_hit_within_ttl_no_re_enum ... ok
test_first_call_enumerates_and_caches ... ok
test_ttl_zero_does_not_overwrite_cache ... ok
test_ttl_zero_no_cache_read_or_write ... ok
test_below_threshold_returns_empty ... ok
test_hit_returns_standard_shape ... ok
test_unreadable_image_returns_empty ... ok
test_main_env_branch ... ok
test_all_noise_no_target ... ok
test_no_rules_no_target ... ok
test_rule_hit_selects_target ... ok
test_both_fail_false ... ok
test_psutil_hit ... ok
test_socket_fallback_hit ... ok
test_count_case_insensitive ... ok
test_exception_returns_zero ... ok
test_report_empty_timeline ... ok
test_report_md_stats_and_slow_flag ... ok
test_shot_saves_png_relative_path ... ok
test_verify_text_command_list_full ... ok
test_verify_text_command_list_no_filter ... ok
test_verify_text_miss_result_fail ... ok
test_verify_text_return_and_timeline_audit ... ok
test_responding_true ... ok
test_timeout_false_after_polling ... ok
----------------------------------------------------------------------
Ran 29 tests in 0.153s
OK
EXITCODE=0
```

### 5.5 全量回归

```text
Ran 56 tests in 0.146s
OK
EXITCODE=0
```

---

## 步骤 6：字符串字面量搜索（重建后重跑，完整原始输出）

```powershell
Set-Location "F:\自动化验证工具\4代码实现\P1_契约与环境适配层"
Get-ChildItem -Recurse -Include *.py | Select-String -Pattern '"ocr"|"uia"|"handle"|"template"'
```

完整命中（36 处）：
```text
contracts\enums.py:4:  硬规则：所有脚本禁止用字符串字面量代替以下枚举（"ocr"/"uia"/"handle" 等视为违规）。
contracts\enums.py:12: HANDLE = "handle"
contracts\enums.py:13: UIA = "uia"
contracts\enums.py:14: OCR = "ocr"
contracts\enums.py:16: TEMPLATE = "template"
contracts\stable_id.py:67: "handle": handle}
contracts\stable_id.py:75: if p["handle"] is not None:
contracts\stable_id.py:76: out.append(("handle", p["handle"]))
contracts\stable_id.py:92: if p["handle"] is not None:
contracts\stable_id.py:93: out.append(("handle", p["handle"]))
contracts\validators.py:21: if p["handle"] is None and p["control_path"] is None \
scripts\locate.py:108: tpl_path=None, prefer="handle"):
scripts\locate.py:144: ap.add_argument("cmd", choices=["ocr", "handle", "template", "all",
scripts\locate.py:155: if a.cmd == "ocr":
scripts\locate.py:158: elif a.cmd == "handle":
scripts\locate.py:161: elif a.cmd == "template":
scripts\timeline.py:53: chain 回退链/诊断链: ["handle","ocr"] 或 ["focus","coords","retry"]"""
tests\test_rebuild_equiv.py:90: self.assertEqual(last["src"], "ocr")
tests\test_rebuild_equiv.py:92: self.assertEqual(last["chain"], ["ocr"])
tests\test_rebuild_equiv.py:113: self.assertEqual(rows[-1]["src"], "ocr")
tests\test_rebuild_equiv.py:207: self.assertEqual(it["source"], "template")
tests\test_rebuild_equiv.py:264: "latency_ms": 100, "src": "handle", "conf": 1.0, "chain": [],
tests\test_rebuild_equiv.py:268: "latency_ms": 300, "src": "handle", "conf": 1.0, "chain": []},
tests\test_rebuild_equiv.py:269: {"ts": "t3", "step": 3, "phase": "verify", "action": "ocr",
tests\test_rebuild_equiv.py:271: "latency_ms": 2500, "src": "ocr", "conf": 0.9,
tests\test_rebuild_equiv.py:272: "chain": ["ocr"]},
tests\test_rebuild_equiv.py:339: self.assertEqual(r["chain"], ["handle"])
tests\test_rebuild_equiv.py:346: self.assertEqual(click_row["src"], "handle")
tests\test_rebuild_equiv.py:348: self.assertEqual(click_row["chain"], ["handle"])
tests\test_stable_id.py:26: self.assertEqual(p["handle"], 0x1A2B)
tests\test_stable_id.py:34: self.assertEqual(p["handle"], 0x1A2B)
tests\test_stable_id.py:40: ["handle", "control_path", "semantic"])
tests\test_stable_id.py:41: self.assertEqual(r[0], ("handle", 0x1A2B))
tests\test_stable_id.py:47: ["semantic", "control_path", "handle"])
tests\test_stable_id.py:60: [("handle", 0x1A2B)])
tests\test_stable_id.py:63: self.assertEqual([l for l, _ in r], ["handle", "control_path"])
tests\test_stable_id.py:68: self.assertEqual(r, [("handle", 0x1A2B),
```

逐条分类：
| 分类 | 数量 | 明细 |
|------|------|------|
| 枚举定义（合规） | 4 | enums.py L12-16 |
| 注释（合规） | 2 | enums.py L4、timeline.py L53 |
| 字典键（合规） | 8 | stable_id.py 5、validators.py 1、tests/test_stable_id.py 2（p["handle"]） |
| CLI 参数名（合规） | 7 | locate.py L108/144/155/158/161 |
| 测试断言（验证枚举值序列化，合规） | 15 | test_rebuild_equiv.py 10、test_stable_id.py 5 |
| **真违规** | **0** | — |

退出码：**0**。结论：重建后字符串搜索无真违规（此前用户自查 11 处命中均属豁免类；本次为重建后权威扫描）。

---

## 步骤 7：运行依赖与 pyc 一致性比对

### 7.1 脚本

`compare_deps_pyc.py`（模块级 co_names + 函数集合 + 函数 co_code）+ `diff_deps_pyc.py`（逐指令定位）。

### 7.2 完整输出（compare_deps_pyc.py）

```text
### cache.py
模块级 co_names 长度: 原始 33 / F盘 33
仅原始有: [] / 仅F盘有: []
函数集合 仅原始有: [] / 仅F盘有: []
同名函数 co_code 全部一致
模块级 co_code 一致: True

### ocr.py
模块级 co_names 长度: 原始 10 / F盘 10
仅原始有: [] / 仅F盘有: []
函数集合 仅原始有: [] / 仅F盘有: []
同名函数 co_code 不一致: ['_setup_gpu', 'load_ocr', 'recognize', 'main']
模块级 co_code 一致: True

### sendinput.py
模块级 co_names 长度: 原始 49 / F盘 49
仅原始有: [] / 仅F盘有: []
函数集合 仅原始有: [] / 仅F盘有: []
同名函数 co_code 不一致: ['type_ascii', 'to_clipboard', 'paste_at', 'main']
模块级 co_code 一致: True
```

### 7.3 差异定位（diff_deps_pyc.py 关键输出）

```text
### ocr.py
- _setup_gpu: 原始 81 指令 / F盘 102 指令 | 原始局部名无 ctypes（F盘有 nvapi 探测）
- load_ocr: 原始 62 指令 / F盘 58 指令 | 原始有 no_angle 参数（F盘硬编码 True）
- recognize: 原始 260 指令 / F盘 294 指令 | 原始无 tmp 文件（F盘存临时文件再识别）
- main: 原始 229 指令 / F盘 217 指令 | 行号错位，参数区差异

### sendinput.py
- type_ascii: 原始 38 指令 / F盘 71 指令 | F盘支持大小写 shift（原始无 vk）
- to_clipboard: 原始 226 指令 / F盘 169 指令 | 原始含 last_err 错误收集（F盘 3 次重试）
- paste_at: 原始先 to_clipboard / F盘先 click
- main: 原始局部名含 text（F盘 paste 分支无文本参数）
```

### 7.4 判定（关键：hash 佐证"F盘=桌面当前版"）

```text
cache.py : F盘 vs 桌面 => 一致  5A44A6BFF861D4FC80BF00CFFFAAABD4236EE7C971387576DD74E4DB4FA6D3F9
ocr.py : F盘 vs 桌面 => 一致    666ACE4B05E5BC0BFE37C2DD69C2065377DEDA2730A6ACA949B6FDBDE6715B75
sendinput.py : F盘 vs 桌面 => 一致  AEDC02E1EF66CB3FC07DD7947385D1C901269E862AB504F5FAC6BE18A383B506
shot.py : F盘 vs 桌面 => 不一致（F盘为本轮 get_engine 修复版，已记录）
```

**结论**：
1. **cache.py 完全合规**（F盘 == pyc == 桌面，全一致）。
2. **ocr.py / sendinput.py 不是"复制错版本"**：F盘 == 桌面当前版；与 pyc 不一致的原因是 **pyc 缓存（11:17:38）是旧版**（源码后续升级：nvapi 探测、tmp 识别、剪贴板重试、大小写键入），缓存未随源码更新（孤儿缓存）。处置：以 F 盘（桌面当前版）为权威，pyc 标记过期，P2 契约化时统一对齐。
3. shot.py 差异 = 已记录修复。

---

## 步骤 8：证据归档到 F 盘（不再放 Temp）

```powershell
$ev = "F:\自动化验证工具\4代码实现\P1_契约与环境适配层\证据"
New-Item -ItemType Directory -Force -Path $ev
Copy-Item "C:\Users\Administrator\AppData\Local\Temp\full_dis_out.txt" "$ev\full_dis_out.txt" -Force
# 及 compare_pyc.py / diff_pyc.py / dump_full_dis.py / compare_deps_pyc.py / diff_deps_pyc.py / dump_shot_get_engine.py
```

结果（7 个文件）：
```text
compare_deps_pyc.py | 2737 B
compare_pyc.py | 2678 B
diff_deps_pyc.py | 2426 B
diff_pyc.py | 3263 B
dump_full_dis.py | 2229 B
dump_shot_get_engine.py | 1298 B
full_dis_out.txt | 39873 B
```

MIGRATION.md 已追加第 7 节（全函数覆盖 / LRESULT 修复 / 字符串搜索 0 真违规 / 依赖比对结论 / 证据目录）。

## 未执行清单

- 未执行任何 git push / sync（保持停止）
- P2 未开始（等本批验收）
