# P2-3 单源化清单（差异盘点产物，只读盘点，未改任何代码）

> 生成时间：2026-09-20 00:2x（系统 date 实取）
> 盘点对象：A 组「测试控制台」源码 + B 组「项目 scripts」
> 盘点方式：AST 解析（`ast.parse`）+ 源码 grep，全部为本机实跑输出，**无估算值混入实测结论**
> 盘点脚本（可复跑）：`F:\自动化验证工具\.workbuddy\tmp\p23_inventory.py`（函数/克隆级）、`p23_inventory2.py`（能力级）
> 状态：**待用户裁定口径与执行顺序后再动代码**

---

## 一、盘点对象（实测路径与体量）

| 组 | 文件 | 绝对路径 | 行数 | 字节 | 函数 | 类 |
|---|---|---|---|---|---|---|
| A | testbar.py | `C:\Users\Administrator\Desktop\测试控制台\testbar.py` | 978 | 38,748 | 55 | 7 |
| A | diagnose.py | `C:\Users\Administrator\Desktop\测试控制台\diagnose.py` | 312 | 12,680 | 10 | 0 |
| A | perfmon.py | `C:\Users\Administrator\Desktop\测试控制台\perfmon.py` | 416 | 15,838 | 19 | 8 |
| B | cache.py | `...\04-implementation\P1-toolbox-legacy\scripts\cache.py` | 177 | 5,534 | 13 | 0 |
| B | env.py | 同上目录 `env.py` | 373 | 13,497 | 19 | 1 |
| B | locate.py | 同上 `locate.py` | 182 | 7,130 | 8 | 0 |
| B | ocr.py | 同上 `ocr.py` | 189 | 6,656 | 6 | 0 |
| B | sendinput.py | 同上 `sendinput.py` | 259 | 8,573 | 13 | 4 |
| B | shot.py | 同上 `shot.py` | 217 | 8,219 | 10 | 1 |
| B | timeline.py | 同上 `timeline.py` | 245 | 9,808 | 7 | 1 |
| B | uitool.py | 同上 `uitool.py` | 431 | 19,377 | 12 | 0 |

- **A 组合计 1,706 行 / B 组合计 2,073 行 / 全部 3,779 行**
- 补充实测：`06-deployment\P1-pyc-cache-backup` 内另有同名 `.pyc`（技能目录 11 个：cache/diagnose/env/locate/ocr/perfmon/sendinput/shot/testbar/timeline/uitool），源码本体在桌面目录（见上表），**不需要反编译 pyc**。
- **入库状态（2026-09-20 00:31，用户裁定）**：A 组源码已复制入库至 `04-implementation\testbar-console\`（6 个文件：3 个 `.py` + `launcher.vbs` + `启动测试控制台.bat` + `build_exe/setup_compile.py`），SHA256 与桌面原件逐一比对**全部一致**，桌面原件**保留未动**；6 个文件已 `git add` 暂存（未提交）。排除项：`测试控制台.exe`(11.8MB)、`build_exe` 的 `.c/.pyd`、`logs/`、`版本归档/`、`验证报告_cookie_sync_20260916/`。

---

## 二、关键实测结论：重复不在"复制粘贴层"

| 检测项 | 实测结果 |
|---|---|
| 跨组同名函数 | 仅 `_load`（A.perfmon 21stmt / B.cache 7stmt，语义不同）、`main`（8 处，各自 CLI 入口）、`__init__`（各类构造）→ **无实质重复** |
| 函数体 AST 归一化哈希完全一致的克隆块 | **0 个**（A/B 组内、跨组均无） |
| Win32 结构体（ctypes Structure）交集 | **0**：A 用 `APPBAORDATA/BITMAPINFOHEADER/FILETIME/MSG/PROCESS_MEMORY_COUNTERS/_Nvml*`；B 用 `INPUT/INPUTUNION/KEYBDINPUT/MOUSEINPUT/MONITORINFOEXW` |
| Win32 API 调用面交集 | **0**（A 静态可解析出 `shell32.IsUserAnAdmin`；B 为 26 个 user32/kernel32/shcore API） |
| psutil 能力交集 | `process_iter`（A 用于远控检测与监控；B 用于 env_probe / process_count） |
| 顶层大写常量交集 | **仅 1 个**：`KEYEVENTF_KEYUP` |
| 共同 import | argparse / ctypes / json / os / psutil / sys / threading / time（均为标准库，不构成重复） |

> **结论**：A 与 B 是两套**独立实现同一领域能力**的代码，不是互相拷贝。
> 因此「代码去重率 ≥60%」这个 P2-3 验收指标**无法按"克隆行占比"口径达成**（实测克隆行 = 0）。口径需用户裁定，见 §五。

---

## 三、单源化清单（11 项，语义级重复）

### A↔B 跨组重复（9 项）

| # | 能力 | A 组实现（证据） | B 组实现（证据） | 冲突/差异 | 建议单源化落点 |
|---|---|---|---|---|---|
| 1 | **远控软件名单** | `testbar.py:90` `REMOTE_PROCS = ["wujie","sunloginclient","todesk","anydesk","rper","uu","mstsc","qclient"]`（8 项） | `P2-layers\env_adapter\env_probe.py:23` `REMOTE_SOFTWARE = ("wujie","sunloginclient","todesk","anydesk","teamviewer","向日葵","wujie2")`（7 项） | 交集 4（wujie/sunloginclient/todesk/anydesk）；A 独有 4（rper/uu/mstsc/qclient）；B 独有 3（teamviewer/向日葵/wujie2）→ **并集 11 项** | 单一名单（建议落 `config/` 或 contracts 常量），两边引用；**注意 B 组 BUG-4 刚修完，名单变更会影响 framework/security 判定，需回归 56 单测** |
| 2 | **窗口信息（hwnd→pid/进程名/标题/rect）** | `diagnose.py:23/29/54/61` `_pid_of` / `_proc_name` / `_title` / `_rect` | `env.py:109 find_windows` / `env.py:183 window_rect`（26 个 Win32 API 已封） | A 自写 4 个薄封装；B 已有完整实现 | A 改为调用 `env.py` |
| 3 | **完整性级别 / 管理员判定** | `diagnose.py:70 _is_admin` / `diagnose.py:77 _integrity` | `env_probe.py`（advapi32 OpenProcessToken+GetTokenInformation，带 `probe_ms` 与缓存） | 两套 advapi32 判定 | A 改为调用 `env_probe` |
| 4 | **查找目标窗口** | `diagnose.py:163 _find_target_hwnd(target_name)` | `env.py:213 pick_target(rules)`（含 `.noise` 抖动过滤）+ `env.py:109 find_windows` | B 的过滤更强 | A 改为调用 `env.pick_target` |
| 5 | **进程存在性 / 按名找 PID** | `testbar.py:167 proc_running(name)`、`perfmon.py:235 find_pid_by_name(name)`（两处 psutil.process_iter） | `env.py:317 process_count(name)`（psutil） | 三处各自遍历 | 抽 `core/utils`：`find_pid_by_name` / `proc_running`，三处共用 |
| 6 | **GPU/显存读取** | `perfmon.py:108-222` 自写 NVML ctypes（`_NvmlUtil/_NvmlMem/_NvmlProcV2/_NvmlProcV3`）+ `gpu_info()`（约 115 行，实测行号区间） | `ocr.py:23` 起用**官方 pynvml**（含 Windows nvml.dll 预加载兼容段，实测 21 ms） | **A 自造轮子 vs B 用官方库** | A 改为调用 pynvml（B 已有可复用实现，含 Windows 兼容段） |
| 7 | **进程 CPU / 内存** | `perfmon.py:31-107` `FILETIME`+`PROCESS_MEMORY_COUNTERS`+`process_times`+`process_memory_mb`（约 77 行） | B 无对应实现（psutil 可直接覆盖） | — | 改用 psutil 一行级实现，落 `core/utils`（P2-3 是否顺带补，待裁定） |
| 8 | **键名 ↔ VK 映射** | `testbar.py:72-95` `KEY_MAP`(F1–F24/INSERT/HOME/…/A–Z/0–9) + `VK_NAME` + `key_name()`（约 24 行） | `sendinput.py` 仅数字 VK（`INPUT_KEYBOARD/KEYEVENTF_*`，CLI 需传 `0x0D` 这类数字） | B 无名字映射 | 抽 `core/utils.keymap`：`KEY_MAP`/`key_name`，sendinput CLI 支持键名 |
| 9 | **GDI 截图** | `testbar.py:187 gdi_screenshot(path)`（`BITMAPINFOHEADER`+`SRCCOPY`，约 45 行） | `shot.py` 三级降级 WGC>dxcam>mss（实测 mss 即 GDI 路，21.61 ms） | ⚠ 盘点 §2.8 已判定"mss 已是这条 GDI 路，补 PrintWindow/BitBlt 属重复造轮子" | **建议不并入**，A 改调 `shot.py`；是否保留为第四档兜底待裁定 |

### B 组内部重复（2 项）

| # | 能力 | 实现 | 证据 | 建议 |
|---|---|---|---|---|
| 10 | JSON 序列化 default | `locate.py::_jdefault`（5 stmt，hash `6cd31856e3f8`）与 `uitool.py::_jdefault`（4 stmt，hash `fb9a20d91f96`） | 实测 AST 不同 → **已发生实现漂移** | 抽 `core/utils`，两处共用一个 |
| 11 | 点击封装 | `sendinput.py::click(x,y,dbl)`（12 stmt）与 `uitool.py::click(session,x,y,dbl,label)`（9 stmt） | 后者是带审计的上层封装（调前者子进程），**非重复，是分层** | 不动（仅登记，避免误合并） |

---

## 四、跨进程 trace 现状（P2-3 第 3 项，实测）

| 项 | 实测 |
|---|---|
| `uitool.py` 子进程调用点 | **8 处**：行 230 / 282 / 296 / 297 / 298 / 299 / 316 / 392（盘点文档记"8 处"，实测一致 ✅） |
| 是否透传 `--trace-id` | **0 处**（`_sub()` 定义于 `uitool.py:76`，无 trace 参数）→ **Step3 待做** |
| `TraceSpan` 字段 | `P2-layers\contracts\models.py:14-24`：仅 `trace_id / layer / step / ts_start / ts_end / meta` → **缺 `span_id` / `parent_span`**（与详细设计 v1.1 §四一致）→ **Step1 已完成（2026-09-20 00:33）**：两个字段已补，均为默认空串且置于末尾；实跑验证旧 6 参构造不变、`duration_ms` 不变、56 单测 `Ran 56 tests in 0.360s / OK` |
| 时钟实现 | `timeline.py` 已用 `perf_counter` 算 delta + `time.time()` 记墙钟（符合 OTel 推荐，无需改） |

---

## 五、⚠ 待裁定：验收指标「代码去重率 ≥60%」的口径

实测三种可能口径，**结论差异极大**，不敢自行定义：

| 口径 | 计算 | 实测值 |
|---|---|---|
| ① 克隆行占比（字面重复） | 完全相同的代码行 / 全部 3,779 行 | **0%**（AST 克隆 0 个） |
| ② 可单源化能力行 / A 组总行 | (NVML 115 + diagnose 窗口/完整性 ~150 + KEY_MAP 24 + proc_running 20 + remote 27 + gdi 45) ≈ 381 / 1,706 | **≈ 22%** |
| ③ testbar 薄壳化后减少的行 / testbar 原行 | 978 行中能力层外移（GUI 保留） | 需先界定"GUI 保留"范围，**未实测，标注：待定** |

> 我的判断（供参考，非结论）：按 §三 的 11 项做单源化后，A 组 1,706 行里约 380–420 行可删除改为引用；若要逼近 60%，需要把 testbar 的 GUI 层也进一步拆分或把 perfmon/diagnose 整体并入项目（属范围扩大，超出"薄壳改造"原义）。
> **请裁定：按哪个口径验收，或改写验收指标。**

---

## 六、建议执行顺序（待确认，未动代码）

```
Step 0  口径裁定（§五）+ 确认清单 11 项取舍          ← 待裁定（去重率口径）
Step 1  contracts/models.py 补 span_id / parent_span  ✅ 已完成（2026-09-20 00:33，56/56 OK）
Step 1b A 组源码入库（testbar-console/）              ✅ 已完成（2026-09-20 00:31，SHA256 一致）
Step 2  core/utils 落地：keymap / proc / _jdefault    ✅ 已完成（2026-09-20 00:4x，落点：P2-layers/core/utils.py + core/constants.py）
Step 3  uitool.py 8 处 _sub() 透传 --trace-id + timeline span 三元组  ✅ 已完成（2026-09-20 00:4x，实跑验证根 span s0 → 子 span s1）
Step 3b 单测基线升级：56 → 70（test_trace_span 3 + test_core_utils 11）  ✅ 已完成
Step 4  A 组（testbar-console）改引用：env.py / env_probe / pynvml / core.utils  ⬜ 待做（GUI 无单测覆盖，需单独一轮 + 真机 smoke）
Step 5  A 组薄壳化后回归：全量单测 + 测试控制台手工 smoke（需真机）  ⬜ 待做
```

> Step 2 的 `core/utils` **落点未定**（候选：① `P2-layers/core/utils.py` 新建 core 包；② 放 `P2-layers/contracts/utils.py`；③ 建独立 `core/` 顶层包）。属目录结构决策，已上报待裁定。

> 每步执行前建 cache-manager 快照；每步跑 56 单测；A 组源码在桌面目录（不在 git 仓库内），**如需纳入版本控制须先确认**（当前 `C:\Users\Administrator\Desktop\测试控制台` 未被 git 跟踪）。

---

## 七、本次盘点的可复跑证据

```bash
# 函数级 / 克隆级盘点
"C:/Program Files/Python313/python.exe" "F:\自动化验证工具\.workbuddy\tmp\p23_inventory.py"
# 能力级（结构体 / API 面 / 常量）盘点
"C:/Program Files/Python313/python.exe" "F:\自动化验证工具\.workbuddy\tmp\p23_inventory2.py"
# 原始 JSON
F:\自动化验证工具\.workbuddy\tmp\p23_inventory.json
```
