# 反馈日报 2026-09-19

## 0.6 目录树全面整理（05:23 ~ 05:30，用户裁定：根目录只留 README+LICENSE）

- **根目录归位**：11 项 → 仅 `README.md` + `LICENSE`。发布类 → `06-deployment/`；验收证据包 → `05-tests/`；仓库同步脚本 → `07-operations/`；交接/进度/盘点/规划 → 新建 `11-management/`。全部 `git mv`/move，**git status 无任何删除（D=0）**。
- **新建 `11-management/`**（项目管理与规划）：索引 + 盘点 v1/v2 + 接下来的任务.txt + HANDOVER + 进度表 + 整理报告。
- **09-19 取证资产入库**：20 脚本 → `10-env-baseline/_tools/`；11 份原始输出 → `_evidence/`。
- **索引与注释同步**：14 文件 / 24 处引用批量更新；05/06/07/10 索引重写、11 索引与整理报告新建；README 结构表更新；技能侧 `environment.md` 加"权威源以 10-env-baseline 为准"声明（双份 SHA256 一致）；历史文档保留原文 + 勘误注记。
- **重复目录排查**：唯一真实重复 = `runtime/contracts` shim(1) ↔ `P2-layers/contracts`(9)，属代码级，建议 P2-2 收尾处理；技能侧环境参考与环境基线内容相近 → 已加权威源声明。
- 复验：56 单测 OK；根目录仅 2 文件；**未 commit，等用户确认后再提交 GitHub**。

## 0.5 C1/C2 修复 + 目录树整理归类 + 盘点 v2（05:08 ~ 05:20，开发暂停中）

- 用户批准 C1/C2 并追加要求：目录树整理归类、说明/索引/注释全部更新、开发前盘点更新 v2、更新《接下来的任务》。
- **C1/C2**：删除僵尸 `opencv_python-5.0.0.93.dist-info`；从回收站**原样恢复** `nvidia_ml_py3-7.352.0.dist-info`（6 文件）→ `pip list` 与 metadata 视图一致；56 单测 OK。
- **目录树整理**：根目录 `决策日志/` 并入技能决策日志权威源后移除；2 个中文目录英文化（`env-report/`、`evidence/`）；3 个中文文件名英文化；`update_refs.py` 更新 10 个活文档引用；历史文档加勘误注记；修 `log_decision.py` 过期 docstring。**全树中文目录数 = 0**。
- **交付**：`本地离线自动化测试工具_开发前盘点_v2.md`（全部 09-19 实测数据；含 v1→v2 变更摘要、目录树现状、API 面 46 项、事故与处置、10 轮就绪度、硬规则、可复现命令）；`桌面\接下来的任务.txt` 已按现状更新（P2-1 标完成并记录"必须保留 opencv-contrib-python"偏差；P2-2 记录做法差异与缺口；新增环境硬规则章节）。
- **实测新发现**：屏幕 **DPI=96（100%）**，与 v1 记录的 1.25 缩放不一致 → 已写入 v2。
- **待用户确认**："目录树仅存放这2个文件" 的目标位置（项目根 / 盘点所在目录），已给出两种口径，未擅自移动。

## 0.4 API 逐一验证 + 环境全面检测（04:47 ~ 04:56，用户要求开发暂停）

- 用户指令：对目前调用的 API 逐一验证；对开发环境冲突/缺失/功能异常全面检测；**所有开发工作暂停，等指示**。
- **Win32 API（源码 grep 抽 46 项）**：实跑通过 42 / 仅解析 4（剪贴板写入类主动不跑、gdi32 盘点标注未使用）/ **失败 0**。实跑含 EnumWindows(504 窗口)、UIPI 判定链、DPI、SendInput(0 事件)、SendMessageTimeoutW(WM_NULL)、AttachThreadInput 配对。
  - 首轮 5 项"失败"经 `GetProcAddress` 复核确认是**我方脚本绑错 DLL**（Global* 在 kernel32、GetDC 在 user32）→ 归零，并记入经验。
- **Python 库 23/23 全通过**；**外部命令 3/3 正常**（nvidia-smi 147 ms vs pynvml 21 ms；WgcCapture.exe 缺失属盘点已记录）。
- **冲突检测**：`pip check` 仅 1 条（基线原有 cudnn）；无 `~` 僵尸元数据、无元数据孤儿；**新发现** C1 僵尸 `opencv_python-5.0.0.93.dist-info`（`pip list` 与 metadata 视图不一致）、C2 `pynvml` 元数据缺失（功能正常）。
- **功能异常**：导入期告警 0；BUG-1/BUG-3 修复在位；BUG-4（framework='wujie'）、BUG-2（env_adapter 零引用）仍存在。
- **端到端快照**：56 单测 OK、截图 [320,200]、匹配 1.0、差分 0.0、OCR 常驻 8888 ms 冷启动 / 22 条 901→106 ms、环境探测 handle_post+mss、UIA 正常。
- 产出：`10-env-baseline\env-api-and-conflict-check_20260919.md`、`09-feedback\issues\issue_003_env-metadata-residue.md`、原始输出 `10-env-baseline\_evidence\10_env_full_check.txt`、可复现脚本见 `10-env-baseline\_tools\`。
- **待用户拍板**：C1/C2 是否处理。**开发保持暂停。**

---

## 0.3 损失评估（04:37 ~ 04:55，用户裁定：保留审计 + 先评估再决定是否恢复）

- 用户裁定 **保留系统审计**（文件系统审核 + Python313 的 Delete SACL），长期用于同类事件归因。
- **项目源码树零损伤（对照《开发前盘点》§5.0 逐文件行数核对）**：P2-layers `contracts` 9/9、`env_adapter` 7/7、`tests` 4/4 行数与基线**完全一致**；`scripts` 除 P2-1 已记录的 BUG-1/BUG-3 修复（shot.py 190→216、ocr.py 146→188）外全一致；`MIGRATION.md` 250→297 为 P2-2 迁移记录。
- **环境侧**：真实缺失 3,804 个 → **核心文件仅 1 个**（`cv2\opencv_videoio_ffmpeg500_64.dll`，opencv 5.0 headless 遗留，与当前 4.10 无关）；其余为 `.pyc` 3220（自动重建）、空目录条目 571、`.pyi` 6（类型存根）、`.txt` 4、`.dist-info` 2。另有 1,486 条 `~pkg` 卸载残留（垃圾，非功能文件）。
- **44 个涉及包全部可导入（44/44）**；功能回归 7 项全通过（59 模块导入、cv2 全功能、paddle CUDA、OCR 263 条、区域截图 320×200、模板匹配 1.0、UIA 11 节点）、56 单测 OK、`pip check` 仅 1 条基线偏差。
- **结论：不恢复**。理由：无可恢复的功能缺口；回收站存的是旧版本，回灌会造成版本混杂；本环境写入成本 47~126 ms/文件，恢复收益近零。
- 交付物：`10-env-baseline\env-damage-assessment_20260919.md`（含可选动作 A/B/C 与防复发硬规则）；indexes 与 issue_002 已同步更新。
- 评估用可复现脚本：`assess_damage.py` / `list_core_damage.py` / `verify_packages.py` / `final_assessment.py` / `sample_noext.py`。
- 工具经验：**这类大范围扫描必须写成 .py 文件执行**，用 heredoc 传同样代码会出现"回收站扫描返回 0"的诡异差异（已实测复现 2 次），文件方式稳定。

---

## 0.2 第三轮会话（04:14 ~ 04:35）：文件丢失根因取证（用户优先级最高的任务）

- 用户指令：commit 暂不执行；**分辨率说法也要验证**（一切行动建立在代码验证上，包括用户的话）；版本问题一律查官方手册与更新说明、不涉及被调用 API 则无需更新；**暂停所有 P2 开发任务，全力排查文件丢失原因**。
- **分辨率复验（代码证实用户说法）**：`GetSystemMetrics` 主屏/虚拟屏 = 2560×1440；`EnumDisplayMonitors` = 1 台 2560×1440；dxcam 帧 `(1440,2560,3)`；`shot.py` 输出 `[2560,1440]` → 与基线一致。04:0x 那次 3200×1440 是切屏瞬时读数。相关报告已按实测更正。
- **根因链（全链路实测）**：
  1. 删除执行者 = `C:\Program Files\WorkBuddy\resources\vendor\genie-trash\win32-x64.exe`（新开系统审计，事件 4663 抓到；受控自证测试通过）
  2. 删除成本 = **47~126 ms/文件**（回收站条目 +100/100 文件）；对照 `cmd /c del` = **0.74 ms/文件**（+0）→ 慢 **64~170 倍**
  3. 大包卸载必然超时：`paddlepaddle` 3,500 文件 × 47 ms ≈ 165 s（命令超时档位 180/300/420/600 s）
  4. 进程被强杀：`killed=true` **28 次**，ProcessKill 时刻与删除潮末点**精确重合**（01:55:53 与最后一笔删除**同一秒**）
  5. 半删指纹：回收站内 `~umpy`(1108)、`~addlepaddle-3.3.1.dist-info`、`~yautogui`、`~pencv_python_headless-5.0.0.93.dist-info`
  6. 触发命令（沙箱日志原文）：`pip uninstall -y opencv-python opencv-contrib-python openc...`、`pip install --no-deps --force-reinstall "op..."`、`pip install --no-deps "numpy==2.3.5"`、`pip uninstall -y paddlepaddle`（**连做 3 次**，03:32/03:38）
- **排除项（均有实测）**：Defender（cmdlet 不存在、无注册杀软）、沙箱 GC（全程只有"无需删除"172 次）、CLI 文件树清单回滚（只覆盖工作区，Python313 出现 0 次）、凭据检测规则（与删除无关）。
- **损坏量化**：回收站 site-packages 条目 29,592（24,405 已存在＝覆盖替换旧版本，良性）；**5,187 个文件当前缺失，5,186 个可从回收站恢复（132.1 MB）**。
- 交付物：
  - `10-env-baseline\env-incident_20260919_root-cause.md`（根因取证报告，含审计回滚命令）
  - `09-feedback\issues\issue_002_site-packages-file-loss.md`（遗留问题）
  - 可复现工具 6 个：`recycle_forensics.py` / `damage_check.py` / `analyze_waves.py` / `attribute_waves.py` / `watch_files.py` / `restore_from_wheels.py`
  - 规则更新：`SOUL.md` 与 `local-dev-environment\SKILL.md` 增补"用户的话也是待验证输入""版本问题查官方手册、不涉及被调用 API 则不建议升级""本机删除高危特性与禁令"
- **需用户决策**：① 保留还是还原本次开启的审计；② 是否恢复 5,186 个文件（建议按包判断：整包缺失→从回收站恢复；部分缺失→wheel 覆盖法补齐）。

---

## 0. 第二轮会话（03:35 ~ 04:05）：清理 CPU 版 + 绝对规则 + 技能默认化 + 环境全检

- 事件流引用：`09-feedback/timeline.jsonl`（04:00 前后 7 条：action/incident/fix/decision）
- 关键事件：
  - **action** 按要求卸载 CPU 版 `paddlepaddle 3.3.1` → 仅保留 `paddlepaddle-gpu 3.3.1`
  - **incident** 环境事故实测量化：`paddle`/`dxcam` 整目录丢失、14 包元数据+模块丢失、modelscope 等 5 包部分文件丢失、`comtypes.client` 与 `colorama.init` 子模块丢失、`cv2` 目录 03:39 前后文件全失
  - **fix** 覆盖法补回（累计约 12,400 文件；paddlepaddle-gpu 取自官方源 cu126）→ `paddle 3.3.1 cuda=True`、`cv2 4.10.0` 全功能、`pip check` 由 18 条收敛为 1 条（基线原有 cudnn 偏差）
  - **decision** 用户裁定的绝对规则写入 `SOUL.md` 与 `local-dev-environment/SKILL.md`
  - **action** 缓存根归并唯一 + 五技能 SHA256 同步校验 + 更正 SKILL.md 过期 junction 描述
- **持续删除监控实证**：`watch_files.py` 8 个目录 × 16 次采样 / 5 分 9 秒 → 文件数零变化（当前无持续删除；根因仍未定位，不猜测）
- **环境检测**：对照《开发前盘点》§5.3 八项全通过，报告见 `10-env-baseline\env-verify_20260919.md`
- 交付物：
  - `10-env-baseline\env-verify_20260919.md`（新建）+ `env-baseline-index.md`（登记）
  - `C:\Users\Administrator\.workbuddy\SOUL.md`（绝对规则 4 条 + 缓存目录 + 加载机制实测）
  - `00-doubao-llm-prerequisites\.user_skills\` 下 `SKILLS_INDEX.md`、`cache-manager/SKILL.md`、`feedback-logger/SKILL.md`、`local-dev-environment/SKILL.md`、`doubao-prerequisites-index.md`（均已同步 AppData）
  - 修复工具：`F:\自动化验证工具\10-env-baseline\_tools\restore_from_wheels.py`、`watch_files.py`；wheel 归档 `_wheels\`
- 遗留：文件丢失根因未定位；BUG-2/BUG-4 未动；P2-2 第 5/7 步未做；本轮文档改动**尚未 commit**

---

## 1. 会话窗口

- 开始：2026-09-19 01:30（承接 09-18 的 P2-1 执行）
- 结束：2026-09-19 02:35
- git 基线：`a0aeccc`（P2-2 前置整理）
- 权威源：`F:\自动化验证工具`

## 2. 事件流（引用 `09-feedback/timeline.jsonl`）

| 时间 | 类型 | 主题 | 结果 |
|---|---|---|---|
| 02:34:57 | action | P2-1 重跑：OpenCV 收敛为 2 包 | cv2=4.10.0，ximgproc/aruco 可用，56 单测 OK |
| 02:34:58 | fix | BUG-1 区域截图修复复核 | 5 组区域形状全对，像素差异 0.000%~0.068%，CLI 输出 640×360 |
| 02:34:58 | fix | BUG-3 OCR 单例化复核 | 首次 12408 ms → 复用 0.0019 ms；recognize 654→144→140 ms |
| 02:34:59 | incident | 环境事故：nvidia CUDA DLL 与多包文件反复丢失 | 覆盖法修复后 paddle CUDA=True、OCR 恢复；仍有 cublas/cusparse dist-info 缺失 |

## 3. 关键事件

**① OpenCV 依赖清理（重跑）**
- 现状：`opencv-contrib-python 4.10.0.84` + `opencv-python 4.10.0.84`；`opencv-python-headless 5.0.0.93` 已移除（dist-info 移至 `F:\自动化验证工具\10-env-baseline\_local_assets\_backup_opencv_stale\`，未删除）
- 实测：`cv2.__version__=4.10.0`；`ximgproc` / `aruco` / `xfeatures2d` 可用；`matchTemplate=1.0`、`absdiff=0`
- 约束取证：paddlex 的 `ocr-core` extra 依赖列表含 `opencv-contrib-python`，`is_extra_available()` 读 **dist 元数据**而非 import —— 删除该 dist 会导致 PaddleOCR 直接拒绝创建流水线（此前已实测复现）

**② 显存监控 pynvml**
- `ocr.py` 已无 `subprocess` 调用 nvidia-smi（仅注释里出现该词），无 nvapi 死代码
- 实测：pynvml 读 12282 MiB，与 nvidia-smi 一致；**0.0017 ms/次 vs 161.49 ms/次**

**③ BUG-1 区域截图**
- 修复代码在位（shot.py 修改时间 2026-09-19 00:43:54）
- 实测 5 组区域：形状全部正确；像素差异 0.000%~0.068%（差异源是该区域屏幕内容在两次抓取之间刷新，非坐标错误）
- CLI：`--region 640,360,512,288` → 输出 `[512, 288]`（修复前恒为全屏 2560×1440）

**④ BUG-3 OCR 单例**
- `get_ocr()` 首次 12408 ms，复用 0.0019 ms，`o1 is o2 = True`

**⑤ 环境事故（重要）**
- 现象：`nvidia/{cudnn,cublas,cufft,cusparse,cuda_runtime}` 的 `bin/` 目录在 02:01~02:20 之间整目录消失；随后 `modelscope_hub`、`colorama` 也损坏
- 处置：一律用「`pip download` 取 wheel → 解压 → 只补缺失文件 → 覆盖写入」，全程不删除
- 结果：`paddle.is_compiled_with_cuda()=True`，OCR 恢复
- 残留告警：`pip check` 报 `nvidia-cublas-cu12` / `nvidia-cusparse-cu12` / `nvidia-cuda-runtime-cu12` 未安装（**dist-info 没了但 DLL 文件在位**，功能正常）

## 4. 不清晰记录

- **多个包文件反复丢失的根因不清楚** —— 不是磁盘空间（C 盘剩余 81G），未找到 Defender 隔离目录；疑似环境侧（沙箱/安全软件）对大量 DLL 的清理行为。已记录为 incident，建议后续观察并考虑把 CUDA DLL 固化到 F 盘专用目录 + 显式 `add_dll_directory`。
- 上一轮对话（01:30 之后那轮）的具体操作记录缺失，仅能从产物状态反推（该轮把两个 OpenCV 统一到 4.10 并装了 winrt extras）。

## 5. 后续建议

1. OpenCV 是否接受当前 "contrib + python 均为 4.10.0.84"？若要严格只剩 `opencv-python`，必须先解决 paddlex 对 contrib dist 的依赖（升级 paddleocr/paddlex 或接受 cv2 降级）。
2. 建议在 P4-2 基线冻结前重跑 `pip freeze` 覆盖 `10-env-baseline\pip_before_20260918.txt`。
3. 修环境一律用"覆盖法"，禁用 `pip --force-reinstall`（共享命名空间包会连带卸载 numpy）。
4. BUG-2（env_adapter 零引用）与 BUG-4（framework 误判）按计划留到 P2-2，本轮未动（已用 `git diff` 取证）。
